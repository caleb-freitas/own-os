[package]
name = "own_os"
version = "0.1.0"
edition = "2018"

# profile used for `cargo build`
[profile.dev]
# disable stack unwinding on panic
panic = "abort"

# profile used for `cargo build --release`
[profile.release]
# disable stack unwinding on panic
panic = "abort"

[dependencies]
